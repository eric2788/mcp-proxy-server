version: '3.8'
services:
  mcp-proxy:
    build: .
    ports:
      - "3000:3000"  # Next.js app
      - "3006:3006"  # SSE server
    environment:
      - NODE_ENV=production
      - KEEP_SERVER_OPEN=1
      - WEB_URL=http://localhost:3000